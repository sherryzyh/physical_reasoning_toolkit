"""Vendored CMPhysBench Scalable Expression Edit Distance (SEED) — Apache-2.0.

Upstream: https://github.com/CMPhysBench/CMPhysBench @ ``b2cd857`` (``SEED/``).
SEED derives from PHYBench EED (MIT) and the ``zss`` package (BSD); the full
attribution chain is preserved in ``LICENSE`` + ``NOTICE`` (see :doc:`PROVENANCE.md`).
Split into the front-end-free :mod:`.core` (pure ``sympy``/``numpy``; ``pint`` is
lazy) and the :mod:`.frontend` LaTeX pipeline.
"""
